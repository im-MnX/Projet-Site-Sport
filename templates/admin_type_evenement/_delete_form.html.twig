<form method="post" action="{{ path('app_admin_type_evenement_delete', {'id': typeEvenement.idTypeEvenement}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeEvenement.idTypeEvenement) }}">
    <button class="btn btn-danger"><i class="fa-solid fa-trash"></i> Supprimer</button>
</form>
