<form method="post" action="{{ path('app_admin_type_evenement_delete', {'id': type_evenement.idTypeEvenement}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_evenement.idTypeEvenement) }}">
    <button class="btn btn-danger"><i class="fa-solid fa-trash"></i> Supprimer</button>
</form>
