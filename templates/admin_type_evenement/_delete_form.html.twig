<form method="post" action="{{ path('app_admin_type_evenement_delete', {'id': type_evenement.idTypeEvenement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_evenement.idTypeEvenement) }}">
    <button class="btn">Delete</button>
</form>
