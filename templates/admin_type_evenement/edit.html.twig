{% extends 'admin_base.html.twig' %}
{% block title %}Modifier le Type{% endblock %}
{% block page_title %}Modifier le Type{% endblock %}
{% block breadcrumb %}<span>Administration</span> / <a href="{{ path('app_admin_type_evenement_index') }}">Types</a> / <span>Modifier</span>{% endblock %}
{% block body %}
<div class="card" style="max-width: 800px; margin: 0 auto;">
    <div class="card-header">
        <h2 class="card-title">Modifier {{ typeEvenement.libelleTypeEvenement }}</h2>
        <div style="display: flex; gap: 0.5rem;">
            <a href="{{ path('app_admin_type_evenement_index') }}" class="btn btn-secondary"><i class="fa-solid fa-arrow-left"></i> Retour</a>
            {{ include('admin_type_evenement/_delete_form.html.twig') }}
        </div>
    </div>
    {{ include('admin_type_evenement/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
</div>
{% endblock %}
