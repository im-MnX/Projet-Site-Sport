{% extends 'admin_base.html.twig' %}
{% block title %}Modifier le Contenu Inscription{% endblock %}
{% block page_title %}Modifier la Page Inscription{% endblock %}
{% block breadcrumb %}<span>Administration</span> / <a href="{{ path('app_admin_inscription_index') }}">Inscription</a> / <span>Modifier</span>{% endblock %}
{% block body %}
<div class="card" style="max-width: 900px; margin: 0 auto;">
    <div class="card-header">
        <h2 class="card-title">Modifier le contenu de la page inscription</h2>
        <div style="display: flex; gap: 0.5rem;">
            <a href="{{ path('app_admin_inscription_index') }}" class="btn btn-secondary"><i class="fa-solid fa-arrow-left"></i> Retour</a>
            {{ include('admin_inscription/_delete_form.html.twig') }}
        </div>
    </div>
    {{ include('admin_inscription/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
</div>
{% endblock %}
